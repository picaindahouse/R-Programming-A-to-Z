# This lesson we will learn how to use some variables

# How to clean up some things:
# To clean up the console- just press the broom 
# It is located at the top right hand corner of the console
# To clean up the global environment press the broom beside import dataset

x = 5
y = 4
# Can run a few lines of code at the same time by highlighting them

z = x + y
z

w = sqrt(z)   # Can use square root in r
w 

tam = 'Yo'
tom = 'Hello'
tim = 'bob'
# Can use paste to combine characters (strings) together
Message = paste(tom,tim,tam)
Message

# Clear everything before leaving by pressing the brooms